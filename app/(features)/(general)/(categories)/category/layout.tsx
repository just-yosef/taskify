import { Header } from "@/app/(features)/(protected)/(shared)/_components";
import React from "react";

const layout = ({ children }: LayoutProps<"/category">) => {
  return <>{children}</>;
};

export default layout;
